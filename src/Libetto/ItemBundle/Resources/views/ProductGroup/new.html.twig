{% extends '::base.html.twig' %}
{% block title %}{% trans %}item.productgroup.title.Creation{% endtrans %}{% endblock title  %}
{% block body -%}

<h1>{% trans %}item.productgroup.title.Creation{% endtrans %}</h1>

<form action="{{ path('productgroup_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">
            <li class="icon-save"></li> {% trans %}item.productgroup.action.Save{% endtrans %}
        </button>
    </form>
<hr/>
    <a href="{{ path('productgroup') }}" class="btn">
        <li class="icon-backward"></li> {% trans %}core.Back to the list{% endtrans %}
    </a>
{% endblock %}
